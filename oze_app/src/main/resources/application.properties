spring.datasource.url=jdbc:mysql://localhost:3306/db_oze?useTimezone=true&serverTimezone=UTC
spring.datasource.username=oze_admin
spring.datasource.password=oze_admin123

# Konfiguracja Hibernate
spring.jpa.hibernate.ddl-auto= update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.liquibase.change-log=classpath:db/changelog/changelog.sql



## Wlacz Spring Security
#spring.security.enabled=true
#
#spring:
#security:
#jwt:
#publicKey: ${JWT_PUBLIC_KEY}
#privateKey: ${JWT_PRIVATE_KEY}
#issuer: ${JWT_ISSUER}
#algorithm: ${JWT_ALGORITHM}
#expiry: ${EXPIRY_DURATION}
##The RSA algorithm is being used here.
